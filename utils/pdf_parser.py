"""PDF text extraction utilities."""

def extract_text_from_pdf(pdf_file) -> str:
    """Extract text from PDF file."""
    # Mock extraction
    return """
    METHODS
    We conducted an independent t-test comparing cognitive scores 
    between treatment and control groups. The difference was significant, 
    t(98)=2.43, p=0.03, Cohen's d=0.45.
    
    RESULTS
    ANOVA revealed significant differences between groups, 
    F(2, 147)=3.82, p=0.04, η²=0.12.
    """
